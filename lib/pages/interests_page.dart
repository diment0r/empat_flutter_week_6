import 'package:flutter/material.dart';

class InterestsPageWidget extends StatelessWidget {
  const InterestsPageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Bag'),
    );
  }
}